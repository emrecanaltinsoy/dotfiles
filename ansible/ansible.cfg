[defaults]
# Use YAML output format (replaces deprecated community.general.yaml callback)
stdout_callback = ansible.builtin.default
result_format = yaml

# Use the stdout_callback when running ad-hoc commands.
bin_ansible_callbacks = True

# Add timestamps to tasks execution and overall execution.
# callbacks_enabled = ansible.posix.profile_tasks

# Improves perfomance by reducing network operations when executing Ansible modules on remote servers.
pipelining = True

# Path to roles directory
roles_path = roles

[callback_profile_tasks]
task_output_limit = 5
summary_only = false