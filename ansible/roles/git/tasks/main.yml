---
# tasks file for roles/git

- name: Configure SSH
  ansible.builtin.import_tasks:
    file: configure_ssh.yml
  tags:
    - git
    - ssh
    - security

- name: Configure Git
  ansible.builtin.import_tasks:
    file: configure_git.yml
  tags:
    - git
